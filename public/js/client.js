/*var client = {};
client.socket = io.connect();
var button = document.getElementById('button');
client.socket.emit('nameEvent',{name: button.value});

client.socket.on('newRoom', function(id) {
	console.log(id);
	$scope.room = id;
})
*/